module.exports = function(featureParser) {

    featureParser = (!!featureParser) ? featureParser : require('../featureParser/featureParser')();

    return function featureTranspiler(filepath) {

    };
};
